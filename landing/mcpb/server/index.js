#!/usr/bin/env node
// APIClaw MCP server entry for Claude Desktop Extension (.mcpb).
// Loads the bundled @nordsym/apiclaw package and starts its MCP stdio server.
await import("@nordsym/apiclaw/dist/index.js");
